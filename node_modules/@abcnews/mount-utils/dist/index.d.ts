type MountValue = string;
type BlockMount = Element & {
    tagName: 'DIV';
    id: MountValue;
    dataset: {
        mount: string;
        mountUsed: string | undefined;
    };
};
type Mount = BlockMount;
type SelectMountsOptions = {
    exact?: boolean;
    includeOwnUsed?: boolean;
    markAsUsed?: boolean;
    convertToBlock?: boolean;
};
declare function isMount(x: unknown, value?: string, exact?: boolean): x is Mount;
declare function getMountValue(el: Mount, value?: string): MountValue;
declare function isUsed(mount: Mount): boolean;
declare function useMount(mount: Mount): Mount;
declare function selectMounts(selector?: string, { exact, includeOwnUsed, markAsUsed, }?: SelectMountsOptions): Mount[];

export { Mount, getMountValue, isMount, isUsed, selectMounts, useMount };
